@model IEnumerable<ApplicationCore.Models.Response.GenreModel>

@foreach (var genre in Model)
{
    <a asp-action="Genre" asp-controller="Movies" asp-route-id="@genre.Id" class="dropdown-item"> @genre.Name</a>
}